# ros_voice 语音交互功能包 —— 接口对接文档

> 面向：机械臂团队、视觉/情绪识别团队、底盘团队、**紧急呼叫团队**、系统集成
> 版本：v1.3　　最后更新：2026-08-01
> 维护：语音组
>
> **v1.3 变更（紧急呼叫团队请重点看 §3.4 与 §4.4）**：新增"紧急呼叫中止"链路——
> 紧急侧向 `/voice/listen_mode` 发 `emergency`，语音侧即开始监听用户的中止意图，
> 判定成立则通过 `/command` 下发「紧急呼叫/中止紧急情况」，并由 control_node
> 转发到新话题 `/emergency/abort`。**其他团队的既有接口全部不变。**

本文档描述 `ros_voice` 功能包对外的全部 ROS 2 话题接口及其 JSON 数据契约。
其他部门只需按本文档约定的话题名、消息类型、字段格式收发数据即可完成对接，**无需阅读语音侧源码**。

---

## 1. 系统概览

`ros_voice` 由三个 ROS 2 节点组成，运行在同一套 launch 中：

| 节点 | 角色 | 职责 |
|------|------|------|
| `voice_node` | 语音输入网关 | 麦克风采音 → VAD 断句 → ASR 识别 → 唤醒词过滤 → 输出用户指令文本 |
| `brain_node` | 语义理解大脑 | 接收指令文本 + 视觉/情绪上下文 → 调用大模型 → 口语回复(TTS) + 结构化机械指令 |
| `control_node` | 指令执行器 | 解析机械指令 → 底盘运动(/cmd_vel) + 机械臂抓取(/arm/grasp_command) |

数据主链路：

```
🎤麦克风 → voice_node → /voice/command → brain_node → /command → control_node → /cmd_vel(底盘)
                                              ↑                              ├→ /arm/grasp_command(机械臂)
                          /vision/scene_objects ┤                            └→ /emergency/abort(紧急呼叫)
                          /vision/emotion_context┘
                                              ↓
                                          🔊扬声器(TTS)
```

紧急呼叫中止链路（v1.3 新增）：

```
紧急侧检测到摔倒/呼救
      │
      ├─ 开始拨打紧急电话 / 发送紧急短信
      └─ 发 /voice/listen_mode = "emergency"
                    │
                    ▼
            voice_node 强制开麦 + 免唤醒词
                    │  用户："不用打了，我没事"
                    ▼
            brain_node 中止意图判定（规则 + 大模型兜底）
                    │  判定成立
                    ▼
            /command  [{"actuator":"紧急呼叫","action":"中止紧急情况",...}]
                    │
                    ▼
            control_node ──▶ /emergency/abort ──▶ 紧急侧挂断电话/撤回短信
                    │
                    └──▶ /voice/listen_mode = "emergency_end"（语音侧自动收尾）
```

---

## 2. 对外话题总表

> ⚠️ 所有话题的消息类型统一为 **`std_msgs/msg/String`**（`/cmd_vel` 除外），
> 真正的内容（JSON 或纯文本）都放在 `msg.data` 字段里。

| 话题 | 类型 | 方向 | 发布方 | 订阅方 | 说明 |
|------|------|------|--------|--------|------|
| `/voice/command` | std_msgs/String | 内部 | voice_node | brain_node | 唤醒后的用户指令文本 |
| `/voice/speaking` | std_msgs/String | 内部 | brain_node | voice_node | 播报期间闭麦的成对信号：start/end |
| `/voice/listen_mode` | std_msgs/String | **公开** | brain_node + 任意节点 | voice_node + brain_node | 轮次模式 continuous/command（brain 专用）+ 硬静音 mute/unmute（外部专用）+ **紧急态 emergency/emergency_end（紧急侧专用）** |
| `/command` | std_msgs/String | 内部 | brain_node | control_node | JSON 机械指令数组 |
| `/cmd_vel` | geometry_msgs/Twist | **对底盘** | control_node | 底盘驱动 | 底盘速度控制 |
| `/arm/grasp_command` | std_msgs/String | **对机械臂** | control_node | 机械臂(arm_task_manager) | 抓取目标名称 |
| `/emergency/abort` | std_msgs/String | **对紧急呼叫** | control_node | 紧急呼叫模块 | **中止紧急电话/短信(JSON)，v1.3 新增** |
| `/vision/scene_objects` | std_msgs/String | **对视觉** | 视觉模块 | brain_node | 当前可见物体(JSON) |
| `/vision/emotion_context` | std_msgs/String | **对情绪** | 情绪模块 | brain_node | 当前情绪(JSON) |

**需要其他部门实现的接口（重点）：**
- 视觉团队：发布 `/vision/scene_objects`
- 情绪团队：发布 `/vision/emotion_context`
- 机械臂团队：订阅 `/arm/grasp_command`
- 底盘团队：订阅 `/cmd_vel`（标准 Twist，无特殊约定）
- **紧急呼叫团队：发布 `/voice/listen_mode` 的 `emergency`/`emergency_end`，订阅 `/emergency/abort`**

---

## 3. 输入接口契约（其他部门 → 语音侧）

### 3.1 `/vision/scene_objects` —— 视觉模块发布

**类型**：`std_msgs/msg/String`（`msg.data` 内为 JSON 字符串）
**频率**：约 **0.5 秒/帧（2 Hz）**。**当画面中没有任何物体时，不发布**（靠语音侧 5 秒窗口自然老化）。
**用途**：上报当前桌面可见物体。语音侧**只缓存不立即触发动作**，待用户下一次语音指令时作为上下文使用。

#### 字段说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `objects` | array | ✅ | 当前帧所有识别到的物体；**每个物理物体一个数组元素**（2 个苹果 = 2 个元素） |
| `count` | int | 建议 | 物体总数，应等于 `objects` 长度；缺省时语音侧自动按数组长度计算 |
| `objects[].class_name` | string | ✅ | 物体英文类名，如 `apple`、`banana`、`cup`（语音侧按此字段计数与去重） |
| `objects[].name_zh` | string | ✅ | 物体中文名，如 `苹果`（用于播报） |
| `objects[].graspable` | bool | ✅ | 是否可抓取（true=可抓取，false=仅可对话） |
| `objects[].action` | string | 建议 | `grasp_allowed`（可抓取）或 `dialogue_only`（仅对话）；与 graspable 二选一即可，两者都给最佳 |
| `objects[].confidence` | float | 可选 | 置信度 0~1 |
| `objects[].camera_xyz` | object | 可选 | 相机坐标 `{x,y,z}`，语音侧不使用（抓取定位由机械臂自行处理） |
| `graspable_targets` | array | 可选 | 可抓取物体子集；缺省时语音侧按 `graspable/action` 自行分类 |
| `dialogue_only_objects` | array | 可选 | 仅对话物体子集；同上 |
| `stamp_sec` / `age_sec` / `source` / `event` | - | 可选 | 元信息，语音侧不强制要求 |

> **多物体说明**：桌面可能同时存在**多个不同物体**，且**每类可能有多个**（如 2 个苹果 + 3 个香蕉 + 1 个杯子）。
> 视觉侧只需把每个物理物体作为 `objects` 的一个元素逐个列出即可，语音侧会按 `class_name` 自动分类计数。

#### 示例（2 个苹果 + 1 个杯子）

```json
{
  "event": "scene_objects",
  "source": "/yolo_detections",
  "stamp_sec": 12345.678,
  "age_sec": 0.124,
  "count": 3,
  "objects": [
    {
      "class_name": "apple", "name_zh": "苹果",
      "confidence": 0.9231, "graspable": true, "action": "grasp_allowed",
      "camera_xyz": { "x": 0.1324, "y": -0.0541, "z": 0.4862 }
    },
    {
      "class_name": "apple", "name_zh": "苹果",
      "confidence": 0.8810, "graspable": true, "action": "grasp_allowed",
      "camera_xyz": { "x": 0.2011, "y": -0.0320, "z": 0.4790 }
    },
    {
      "class_name": "cup", "name_zh": "杯子",
      "confidence": 0.8125, "graspable": false, "action": "dialogue_only",
      "camera_xyz": { "x": 0.2189, "y": 0.0312, "z": 0.5033 }
    }
  ],
  "graspable_targets": [
    { "class_name": "apple", "name_zh": "苹果", "graspable": true, "action": "grasp_allowed" },
    { "class_name": "apple", "name_zh": "苹果", "graspable": true, "action": "grasp_allowed" }
  ],
  "dialogue_only_objects": [
    { "class_name": "cup", "name_zh": "杯子", "graspable": false, "action": "dialogue_only" }
  ]
}
```

> 语音侧最小要求：只要 `objects` 数组中每个元素含 `class_name`、`name_zh`、`graspable` 三个字段即可正常工作。

---

### 3.2 `/vision/emotion_context` —— 情绪模块发布

**类型**：`std_msgs/msg/String`（`msg.data` 内为 JSON 字符串）
**频率**：约 **0.5 秒/帧（2 Hz）**。**当画面中没有人脸时，不发布**。
**用途**：上报当前用户情绪。**默认只有一个人，不计数量**。语音侧缓存最新情绪用于对话上下文；
若检测到负面情绪则**自动触发安抚**（详见 §5.2）。

#### 字段说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `emotion` | string | ✅ | 情绪英文标识，如 `neutral`、`happy`、`low_mood`、`negative_distress` |
| `emotion_zh` | string | ✅ | 情绪中文名，如 `平静`、`情绪低落`（用于播报） |
| `intervention_required` | bool | ✅ | 是否需要干预；**当前约定 `low_mood` / `negative_distress` 为 true** |
| `confidence` | float | 可选 | 置信度 0~1 |
| `status` | string | 可选 | `ok` 等状态标识 |
| `stamp_sec` / `source` / `event` / `raw` | - | 可选 | 元信息 |

#### 示例（正常情绪）

```json
{
  "event": "emotion",
  "source": "/emotion/result",
  "stamp_sec": 12350.321,
  "emotion": "neutral",
  "emotion_zh": "平静",
  "confidence": 0.86,
  "status": "ok",
  "intervention_required": false
}
```

#### 示例（需要干预）

```json
{
  "event": "emotion",
  "source": "/emotion/result",
  "stamp_sec": 12360.884,
  "emotion": "low_mood",
  "emotion_zh": "情绪低落",
  "confidence": 0.91,
  "status": "ok",
  "intervention_required": true
}
```

> **触发干预的情绪取值**（当前约定，可扩展）：`low_mood`、`negative_distress`。
> 情绪侧只要把这两类的 `intervention_required` 置为 `true` 即可。

---

### 3.3 `/voice/listen_mode` —— 麦克风唯一控制话题

**类型**：`std_msgs/msg/String`
**发布方**：brain_node（`continuous`/`command`/`emergency_end`）+ 任意外部节点（`mute`/`unmute`）+ 紧急呼叫模块（`emergency`/`emergency_end`）
**订阅方**：voice_node（全部模式）+ brain_node（只认 `emergency`/`emergency_end`）
**用途**：控制 voice_node 的麦克风。六种模式分属**三个正交维度**。

> 原 `/voice/mute` 话题已**移除**，其 `mute`/`unmute` 语义合并至本话题，用法不变，
> 仅需把话题名从 `/voice/mute` 换成 `/voice/listen_mode`。

#### 维度一 · 轮次模式（brain_node 专用，外部节点勿发）

brain_node 每处理完一句话发一次，声明"下一句怎么收"。

| `msg.data` | 模式 | 行为 |
|------------|------|------|
| `"continuous"` | 持续监听 | 麦克风开启，**不需要唤醒词**，下一句直接作为指令（机器人问了问题等用户回答时） |
| `"command"` | 命令模式 | 麦克风开启，**需要先说唤醒词**才能触发指令（默认状态、对话结束后） |

#### 维度二 · 硬静音（外部节点专用）

底盘、机械臂等在动作期间静音麦克风，**任意时刻可发**，不受对话轮次约束。

| `msg.data` | 行为 |
|------------|------|
| `"mute"` | 麦克风彻底关闭（从音频源头截断），**且此后 `continuous`/`command` 一律无法开启** |
| `"unmute"` | 解除硬静音，按最后一次轮次模式恢复 |

> **硬静音是粘性的，只有 `unmute` 能解除。** 即使 `mute` 到达时 brain_node 正思考到一半，
> 它随后发回的 `continuous`/`command` 也不会把麦克风打开（但会记下唤醒词设置，
> `unmute` 后即以该模式恢复）。因此外部节点**必须成对发送 `mute`/`unmute`**，
> 漏发 `unmute` 会导致麦克风永久关闭。

> ⚠️ **brain_node 永远不可以发 `mute`。** 一旦发出，用户无法说话，brain_node 也就
> 永远等不到下一条指令去发 `unmute` —— 直接死锁。`mute`/`unmute` 只属于外部节点。

#### 维度三 · 紧急态（紧急呼叫模块专用，v1.3 新增）

紧急呼叫模块**开始拨打紧急电话/发送紧急短信时**发 `emergency`，**该次紧急流程
以任何方式结束时**发 `emergency_end`。详细时序与判定规则见 §3.4。

| `msg.data` | 行为 |
|------------|------|
| `"emergency"` | 麦克风**强制开启 + 免唤醒词**，并开始对每句话做"中止紧急呼叫"意图判定 |
| `"emergency_end"` | 退出紧急态，恢复进入前的轮次模式与硬静音状态 |

> **紧急态优先级最高，压过硬静音。** 机械臂抓取途中老人摔倒，麦克风此刻正被
> `mute` 压着——若紧急态不能越过它，用户喊"不用打了"根本传不进来。
> 因此 `emergency` 期间硬静音只是被**暂时旁路**（状态仍记着），
> `emergency_end` 后原样恢复，机械臂不必关心紧急流程。
>
> 唯一压得住紧急态的是播报闸：机器人自己出声时照旧闭麦，否则会把自己的声音听回去。

未知值按 `command` 处理（fail-open），宁可多要一次唤醒词，也不把麦克风锁死。

#### 示例

> ⚠️ **命令行测试必须加 `--once`。** `ros2 topic pub` 默认以 **1 Hz 无限循环**发送
> （`-r` 默认为 1），不加 `--once` 会导致 `mute` 每秒重发一次：此时在另一个终端发
> `unmute` 也没用，一秒内就会被重新静音。代码里用 `publish()` 发一次即可，无此问题。

```bash
# 机械臂开始抓取时静音
ros2 topic pub --once /voice/listen_mode std_msgs/msg/String '{data: "mute"}'

# 抓取完成后解除静音（必须成对发送！）
ros2 topic pub --once /voice/listen_mode std_msgs/msg/String '{data: "unmute"}'
```

---

### 3.4 紧急呼叫中止 —— 紧急呼叫模块对接 ⭐（v1.3 新增）

**紧急侧要做两件事**：进入紧急流程时发 `emergency`，结束时发 `emergency_end`；
同时订阅 `/emergency/abort`（契约见 §4.4）。

#### 时序

| 步 | 谁 | 动作 |
|----|----|------|
| 1 | 紧急侧 | 检测到摔倒/呼救，**开始拨打紧急电话或发送紧急短信** |
| 2 | 紧急侧 | 向 `/voice/listen_mode` 发 `"emergency"`（与第 1 步同时，不要等电话拨通） |
| 3 | 语音侧 | 强制开麦、免唤醒词，对听到的每句话判定"用户是否要中止求助" |
| 4 | 语音侧 | 判定成立 → `/command` 下发中止指令 → control_node 转发 `/emergency/abort` |
| 5 | 紧急侧 | 收到 `/emergency/abort`，**挂断电话 / 撤回短信** |
| 6 | 双方 | 语音侧自动补发 `emergency_end`；紧急侧在流程结束时也发一次（幂等，重复无害） |

#### 紧急侧发布示例

```bash
# 开始拨打紧急电话时
ros2 topic pub --once /voice/listen_mode std_msgs/msg/String '{data: "emergency"}'

# 紧急流程结束时（无论是打完了、超时了、还是被中止了）
ros2 topic pub --once /voice/listen_mode std_msgs/msg/String '{data: "emergency_end"}'
```

```python
# Python 节点内（注意用 publish 发一次即可，不要用 ros2 topic pub 的循环模式）
self._mode_pub = self.create_publisher(String, "/voice/listen_mode", 10)
self._mode_pub.publish(String(data="emergency"))      # 拨号开始
...
self._mode_pub.publish(String(data="emergency_end"))  # 流程结束
```

#### 中止意图怎么判定的

分两级，**规则优先、大模型兜底**（源码：`voice_brain_module/emergency.py`）：

| 级别 | 判定方式 | 耗时 | 覆盖 |
|------|----------|------|------|
| 规则层 | 关键词/否定词邻近匹配，纯离线 | 微秒级 | "不用打了"、"我没事"、"取消"、"按错了"、"别麻烦人家" |
| 大模型层 | 规则拿不准时后台调用大模型仲裁 | 约 1 秒 | 老人松散口语，如"哎呀不至于，我好着呢" |

规则层能独立完成判定，**不依赖网络**——紧急场景下网络本身可能就是不通的，
而"取消"这个动作恰恰不能等网络。

> **判定取向是刻意不对称的**：
> - 漏判（该中止没中止）＝ 多打一通电话，虚惊一场，代价可控
> - 误判（不该中止却中止）＝ 真实求救被撤销，可能危及生命
>
> 因此：**求救加强句（"救命"、"快叫救护车"、"我不行了"、"起不来"）对取消句有一票否决权**；
> 两者同时出现（"我没事，不过你快叫救护车"）交大模型仲裁；大模型超时/异常/拿不准
> 一律判定为"继续呼叫"。**语音侧宁可不撤，也不误撤。**

#### 紧急侧必须知道的四件事

1. **`emergency` / `emergency_end` 必须成对发送。** 漏发 `emergency_end` 的后果比漏发
   `unmute` 更糟：麦克风被钉在"强制开麦 + 免唤醒词"，机械臂/底盘的 `mute` 也压不住它。
   语音侧有 **180 秒兜底超时**会自动退出并打 ERROR 日志，但那是安全网，不是正常流程。
2. **中止指令只发一次。** 同一次紧急流程内，语音侧判定成立后即退出紧急态，
   不会重复下发；紧急侧若要再次监听，需重新发 `emergency`。
3. **紧急态期间机器人不与用户闲聊。** 只做中止判定，不调大模型对话、不执行其他语音指令
   ——避免抢占扬声器和麦克风，干扰正在进行的通话。中止成功后机器人会说一句
   "好的，已经取消紧急呼叫了。您要是不舒服，随时告诉我。"
4. **中止是"语音侧的判定结论"，不是"用户身份认证"。** 语音侧无法分辨说话的是老人本人
   还是旁人。若紧急侧对撤销有更高的确认要求（如需二次确认、需家属授权），
   请在收到 `/emergency/abort` 后自行追加确认流程。

---

## 4. 输出接口契约（语音侧 → 其他部门）

### 4.1 `/command` —— brain_node 发布（内部，control_node 消费）

**类型**：`std_msgs/msg/String`（`msg.data` 内为 **JSON 数组**）

每条指令为统一的三段式结构：

```json
{ "actuator": "执行机构", "action": "动作", "params": { ... } }
```

一次可包含多条指令（按数组顺序依次执行）。空数组 `[]` 表示无机械动作（纯对话）。

#### 完整指令集（COMMANDS）

| actuator | action | params | 取值范围/选项 |
|----------|--------|--------|--------------|
| 底盘 | 前进 | speed, distance | speed 0.05~0.5 m/s；distance 0.1~10 m |
| 底盘 | 后退 | speed, distance | speed 0.05~0.3 m/s；distance 0.1~5 m |
| 底盘 | 左转 | angle, speed | angle 5~360°；speed 0.1~1.0 rad/s |
| 底盘 | 右转 | angle, speed | angle 5~360°；speed 0.1~1.0 rad/s |
| 底盘 | 停止 | （无） | — |
| 底盘 | 人体跟踪 | 状态 | 开始 / 停止 |
| 机械臂 | 抓取 | target | 苹果 / 香蕉 / 瓶子 / 蛋糕 / 小黄鸭 / 绿色药盒 / 大樱桃 |

#### 示例

```json
[
  {"actuator":"底盘","action":"前进","params":{"speed":0.2,"distance":1.0}},
  {"actuator":"机械臂","action":"抓取","params":{"target":"苹果"}}
]
```

> 指令由大模型生成，并经 `voice_brain_module/commands.py` 的 `validate_commands()` 严格校验：
> 执行机构/动作/参数必须命中指令集，否则自动让大模型重试（最多 2 次），仍失败则丢弃该指令。

#### 系统级指令（不由大模型生成，v1.3 新增）

| actuator | action | params | 触发条件 |
|----------|--------|--------|----------|
| 紧急呼叫 | 中止紧急情况 | reason, detector, utterance | 紧急态下判定用户明确要中止求助（§3.4） |

```json
[{"actuator":"紧急呼叫","action":"中止紧急情况",
  "params":{"reason":"user_cancel","detector":"rule","utterance":"不用打了，我没事"}}]
```

> ⚠️ 这条指令**故意不出现在大模型的提示词里**，`validate_commands()` 也不接受它——
> 它由确定性的代码逻辑直接下发。让大模型有机会生成它风险太大：聊着家常突然
> 幻觉出一条"中止紧急情况"，紧急侧无从分辨真假。

---

### 4.2 `/arm/grasp_command` —— 机械臂团队订阅 ⭐

**类型**：`std_msgs/msg/String`
**内容**：`msg.data` 为**抓取目标的中文名称字符串**（非 JSON）。

| 可能取值 | 苹果 / 香蕉 / 瓶子 / 蛋糕 / 小黄鸭 / 绿色药盒 / 大樱桃 |
|----------|------|

#### 示例

```
msg.data = "苹果"
```

**机械臂团队职责**：
- 订阅本话题，收到目标名后自行完成"目标定位 → 手眼标定 → 抓取"全过程。
- 目标到检测话题的映射、常驻 YOLO 还是 detector，**均由机械臂侧决定**，语音侧不关心。
- 若桌面有多个同名物体（如 2 个苹果），由机械臂侧自行决定抓取哪一个。

> 参考：机械臂侧自动抓取管理由 `arm_task_manager.py` 处理，其内部映射示例：
> 苹果/香蕉/瓶子/蛋糕 → 常驻 YOLO `/detect_yolo/<class>_position`；
> 小黄鸭/绿色药盒/大樱桃 → detector `/duck_position`、`/box_position`、`/red_circle_position`。

---

### 4.3 `/cmd_vel` —— 底盘团队订阅

**类型**：`geometry_msgs/msg/Twist`（标准消息，无特殊约定）

control_node 按指令计算 `linear.x`（前进/后退，正为前）与 `angular.z`（转向，正为左），
按"距离/速度=时长"持续发布，到时或收到停止后发布全零 Twist。

---

### 4.4 `/emergency/abort` —— 紧急呼叫团队订阅 ⭐（v1.3 新增）

**类型**：`std_msgs/msg/String`（`msg.data` 内为 JSON 字符串）
**触发**：紧急态下语音侧判定用户要中止求助，**一次紧急流程最多发一条**。
**语义**：请立即挂断正在拨打的紧急电话 / 撤回正在发送的紧急短信。

#### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| `event` | string | 固定为 `emergency_abort` |
| `action` | string | 固定为 `中止紧急情况` |
| `reason` | string | 中止原因，当前只有 `user_cancel`（用户主动取消） |
| `detector` | string | 判定来源：`rule`（关键词规则）/ `llm`（大模型语义判定） |
| `utterance` | string | 触发判定的用户原话（最长 100 字），**建议紧急侧留档**，便于事后复核撤销是否合理 |
| `stamp_sec` | float | 转发时刻的 Unix 时间戳 |

#### 示例

```json
{
  "event": "emergency_abort",
  "action": "中止紧急情况",
  "reason": "user_cancel",
  "detector": "rule",
  "utterance": "不用打了，我没事",
  "stamp_sec": 1785583532.677988
}
```

#### 紧急团队职责

- 订阅本话题，收到后挂断电话 / 撤回短信，并结束本次紧急流程。
- 收到后**仍建议回发一次 `emergency_end`**（语音侧已自动补发，重复无害且幂等）。
- 若挂断失败（如电话已接通、短信已发出），请自行决定后续策略，语音侧不再重发。

> **延迟保证**：control_node 收到 `/command` 后在 ROS 回调线程里**立即**转发本话题，
> 不进指令执行队列——否则一条"前进 10 米"能把中止指令堵在后面 50 秒，等于没做。
> 从用户说完话到本话题发出，典型延迟：规则命中约 0.1 秒内，大模型仲裁约 1~2 秒
> （不含 ASR 识别耗时）。

---

## 5. 语音侧内部处理逻辑（供理解，非接口）

### 5.1 视觉/情绪数据的"缓存—去抖—注入"机制

- `scene_objects` 与 `emotion_context` **本身不触发任何动作**，只把当前状态缓存进**最近 3 秒滑动窗口**。
- 用户**下一次语音指令**到来时，把去抖后的稳定状态作为上下文一并喂给大模型，
  使其能回答"桌上有什么"、"帮我抓那个苹果"等需要视觉的指令。
- **去抖算法**（消除偶发异常帧）：3 秒窗口内做**众数投票**。窗口足够短，对真实变化
  （物体被拿走、情绪切换）的反应延迟最多约 1.5 秒。
  - **情绪**：取窗口内众数（需占多数且 ≥2 帧）作为稳定情绪。例：连续开心中夹 1 帧伤心 → 判定开心，丢弃异常帧。
  - **物体**：仅保留在窗口内出现帧数 **≥50%** 的物体类，闪烁误检（仅 1 帧）被滤除；
    每类数量取各帧出现数量的众数（支持多类、每类多个）。

### 5.2 负面情绪自动干预（唯一由视觉话题主动触发的动作）

- 当**稳定情绪**的 `intervention_required = true` 时，brain_node 主动调用大模型生成安抚话术并播报。
- 带 **30 秒冷却**，避免重复打扰。不产生任何机械指令。
- 干预播报期间 brain_node 会发 `/voice/speaking` 的 `start`/`end`，麦克风照常闭合。
- 干预有 **3 秒时效**：brain_node 串行处理，若干预入队时正在处理用户指令（LLM+TTS
  通常数秒），排到时已超时则**直接丢弃**——迟到的安慰比不安慰更出戏。
- 被丢弃的干预会**退还 30 秒冷却**，不占用下一次干预机会。

### 5.3 紧急态为什么走"快车道"

brain_node 的工作队列是**串行**的：一次大模型推理加 TTS 播报动辄十几秒，放歌能占
到一分钟。若把中止判定排在队列里，用户喊了"不用打了"却要等一首歌唱完才生效——
紧急场景下这等于没做。

因此紧急态下的每句话**不进队列**，在 ROS 回调线程里当场判定并下发（规则匹配是
微秒级的，不会拖慢执行器）；只有事后那句口头确认才回队列排队，那时电话已经撤了，
说话晚几秒无妨。control_node 侧同理：中止指令在回调里直接转发 `/emergency/abort`，
不进指令执行队列。

### 5.4 大模型输出格式（内部约定）

大模型被要求严格输出两段式：

```
[口语回复]：
<对用户的自然口语回答，经 TTS 播报>
[执行指令]：
<JSON 指令数组，见 §4.1>
```

---

## 6. 运行方式

```bash
# 环境
source /opt/ros/humble/setup.bash
source ~/ros2_ws/install/setup.bash

# 大模型 API Key（必须设置，无默认值）
export DASHSCOPE_API_KEY=sk-xxxxxxxx

# 一键启动三节点
ros2 launch ros_voice voice.launch.py

# 或单独启动
ros2 run ros_voice voice_node
ros2 run ros_voice brain_node
ros2 run ros_voice control_node
```

### 联调自测命令

> ⚠️ **`ros2 topic pub` 默认 1 Hz 无限循环发送**（`-r` 默认为 1），加 `--once` 才只发一次。
> 下面两类命令**恰好相反**，别弄混：
>
> | 类别 | 话题 | 要不要 `--once` | 原因 |
> |------|------|----------------|------|
> | 控制/指令 | `/voice/listen_mode`、`/voice/command` | ✅ **必须加** | 不加会每秒重复触发。`mute` 会压住 `unmute`；`command` 会每秒灌一条指令，无限调用大模型 |
> | 视觉感知 | `/vision/scene_objects`、`/vision/emotion_context` | ❌ **不要加** | 去抖窗口要求 3 秒内**至少 2 帧**同类数据才算"稳定"，只发一帧永远不会触发 |

```bash
# 模拟视觉模块发布物体（2 个苹果 + 1 个杯子）
# 不加 --once：需持续发布，攒够 2 帧才会被判为稳定物体
ros2 topic pub /vision/scene_objects std_msgs/msg/String \
  '{data: "{\"objects\":[{\"class_name\":\"apple\",\"name_zh\":\"苹果\",\"graspable\":true,\"action\":\"grasp_allowed\"},{\"class_name\":\"apple\",\"name_zh\":\"苹果\",\"graspable\":true,\"action\":\"grasp_allowed\"},{\"class_name\":\"cup\",\"name_zh\":\"杯子\",\"graspable\":false,\"action\":\"dialogue_only\"}]}"}'

# 模拟情绪模块发布负面情绪（触发自动安抚）
# 同样不加 --once：单帧不满足"稳定情绪"阈值，不会触发干预
ros2 topic pub /vision/emotion_context std_msgs/msg/String \
  '{data: "{\"emotion\":\"low_mood\",\"emotion_zh\":\"情绪低落\",\"intervention_required\":true}"}'

# 外部静音麦克风（任意节点可用）
ros2 topic pub --once /voice/listen_mode std_msgs/msg/String '{data: "mute"}'

# 外部解除静音
ros2 topic pub --once /voice/listen_mode std_msgs/msg/String '{data: "unmute"}'

# 直接给 brain 灌指令文本（跳过语音）
ros2 topic pub --once /voice/command std_msgs/msg/String '{data: "帮我抓个苹果"}'

# 观察机械臂抓取输出
ros2 topic echo /arm/grasp_command

# 观察底盘输出
ros2 topic echo /cmd_vel
```

#### 紧急呼叫中止联调（v1.3）

```bash
# 终端 1：盯着紧急中止输出
ros2 topic echo /emergency/abort

# 终端 2：模拟紧急侧进入紧急态（相当于开始拨打紧急电话）
ros2 topic pub --once /voice/listen_mode std_msgs/msg/String '{data: "emergency"}'

# 终端 2：模拟用户说话（跳过麦克风，直接灌文本）
ros2 topic pub --once /voice/command std_msgs/msg/String '{data: "不用打了，我没事"}'
#   → 终端 1 应立刻收到 emergency_abort，detector 为 rule

# 反例：求救加强句绝不能触发中止
ros2 topic pub --once /voice/listen_mode std_msgs/msg/String '{data: "emergency"}'
ros2 topic pub --once /voice/command std_msgs/msg/String '{data: "快叫救护车"}'
#   → 终端 1 应**没有任何输出**，brain_node 日志打印"不构成中止，继续呼叫"

# 手动退出紧急态（中止成功时语音侧会自动发，此处用于反例收尾）
ros2 topic pub --once /voice/listen_mode std_msgs/msg/String '{data: "emergency_end"}'
```

> 判定过程在 brain_node 日志里可查：`判定中止紧急呼叫（命中…）` / `不构成中止` /
> `规则拿不准，交大模型判定`。麦克风状态在 voice_node 日志里可查：
> `进入紧急态：强制开麦 + 免唤醒词` / `退出紧急态（…）`。

---

## 7. 关键配置项（voice_brain_module/config.py）

| 配置 | 默认值 | 说明 |
|------|--------|------|
| `DASHSCOPE_API_KEY` | （必填，无默认值） | 阿里云 DashScope API Key，通过环境变量设置 |
| `WAKE_WORD` | `小智小智` | 唤醒词（支持短词「小智」及拼音模糊匹配） |
| `LLM_MODEL` | `qwen-turbo` | 大模型（阿里云 DashScope） |
| `TTS_VOICE` | `longxiaochun_v2` | CosyVoice v2 音色 |
| 滑动窗口 | 3 秒 | brain_node 视觉/情绪去抖窗口（众数投票） |
| 情绪干预冷却 | 30 秒 | 两次自动安抚最小间隔 |
| `_EMERGENCY_MAX_SEC` | 180 秒 | 紧急态兜底超时（`voice_node.py`），防紧急侧漏发 `emergency_end` |
| 中止判定规则 | — | `voice_brain_module/emergency.py`，改关键词只需改此文件 |

---

## 8. 对接清单（Checklist）

各团队对接前请逐项确认：

- [ ] **视觉团队**：发布 `/vision/scene_objects`（String/JSON），含 `objects[]`，每个物体有 `class_name`+`name_zh`+`graspable`；无物体时不发布。
- [ ] **情绪团队**：发布 `/vision/emotion_context`（String/JSON），含 `emotion`+`emotion_zh`+`intervention_required`；负面情绪置 `intervention_required=true`；无人脸时不发布。
- [ ] **机械臂团队**：订阅 `/arm/grasp_command`（String），`data` 为中文目标名，七选一；自行处理定位与抓取。动作前后**必须成对**向 `/voice/listen_mode` 发 `"mute"`/`"unmute"`（漏发 unmute 会导致麦克风永久关闭）。
- [ ] **底盘团队**：订阅 `/cmd_vel`（Twist），标准速度控制。运动前后**必须成对**向 `/voice/listen_mode` 发 `"mute"`/`"unmute"`（漏发 unmute 会导致麦克风永久关闭）。
- [ ] **紧急呼叫团队**（v1.3 新增）：
  - [ ] 开始拨打紧急电话/发送紧急短信时，向 `/voice/listen_mode` 发 `"emergency"`（String）
  - [ ] 紧急流程以任何方式结束时，发 `"emergency_end"`（**必须成对**，语音侧 180 秒兜底超时只是安全网）
  - [ ] 订阅 `/emergency/abort`（String/JSON），收到后挂断电话/撤回短信
  - [ ] 确认已读 §3.4 的"必须知道的四件事"，尤其是"中止是判定结论，不是身份认证"
  - [ ] 与语音组联调过正例（"不用打了"→ 应中止）与反例（"快叫救护车"→ 绝不能中止）
- [ ] **集成**：确认话题名、消息类型完全一致；`DASHSCOPE_API_KEY` 已配置；音频设备名匹配正确。
