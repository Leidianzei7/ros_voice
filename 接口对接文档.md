# ros_voice 语音交互功能包 —— 接口对接文档

> 面向：视觉/情绪团队、机械臂团队、底盘团队、紧急呼叫团队、系统集成
> 版本：v1.3　　最后更新：2026-08-01　　维护：语音组

本文档只描述 `ros_voice` 对外的话题接口与数据契约。按此收发即可完成对接，**无需阅读语音侧源码**。
内部实现细节见仓库 `README.md`。

**v1.3 变更**：新增紧急呼叫中止链路（§3.4、§4.4），紧急团队为新增对接方。
**其他团队的既有接口全部不变。**

---

## 1. 系统概览

三个节点跑在同一套 launch 中：`voice_node`（采音+识别）→ `brain_node`（大模型理解+播报）→ `control_node`（指令执行）。

```
🎤 → voice_node → brain_node → control_node ─┬→ /cmd_vel            (底盘)
                       ↑    │                ├→ /arm/grasp_command  (机械臂)
   /vision/scene_objects┤    │                └→ /emergency/abort    (紧急呼叫·中止)
 /vision/emotion_context┘    └──────────────────→ /emergency/initiate (紧急呼叫·发起)
```

机器人当前能执行的动作：底盘前进/后退/左转/右转/停止/人体跟踪、机械臂抓取（七种目标）、播放歌曲。

---

## 2. 对外话题总表

> 除 `/cmd_vel` 外，所有话题的消息类型统一为 **`std_msgs/msg/String`**，内容（JSON 或纯文本）放在 `msg.data` 里。

| 话题 | 方向 | 谁发 | 谁收 | 说明 |
|------|------|------|------|------|
| `/vision/scene_objects` | **视觉 → 语音** | 视觉模块 | brain_node | 当前可见物体(JSON) |
| `/vision/emotion_context` | **情绪 → 语音** | 情绪模块 | brain_node | 当前用户情绪(JSON) |
| `/voice/listen_mode` | **外部 → 语音** | 机械臂/底盘/紧急侧 | voice_node + brain_node | 麦克风控制 + 紧急态 |
| `/command` | **语音 → 多方** | brain_node | control_node + 机械臂 + 人体跟踪 | 指令总线(JSON 数组)，各方按 actuator 过滤 |
| `/arm/grasp_command` | **语音 → 机械臂** | control_node | 机械臂 | 抓取目标中文名 |
| `/cmd_vel` | **语音 → 底盘** | control_node | 底盘驱动 | `geometry_msgs/Twist` 速度控制 |
| `/emergency/initiate` | **语音 → 紧急** | brain_node | 紧急呼叫模块 | **发起**紧急电话/短信(JSON) |
| `/emergency/abort` | **语音 → 紧急** | control_node | 紧急呼叫模块 | **中止**紧急电话/短信(JSON) |

各团队要做的事：

| 团队 | 发布 | 订阅 |
|------|------|------|
| 视觉 | `/vision/scene_objects` | — |
| 情绪 | `/vision/emotion_context` | — |
| 机械臂 | `/voice/listen_mode` 的 `mute`/`unmute` | `/arm/grasp_command`（或直接过滤 `/command`） |
| 底盘 | `/voice/listen_mode` 的 `mute`/`unmute` | `/cmd_vel` |
| 人体跟踪 | `/voice/listen_mode` 的 `mute`/`unmute` | `/command` 中 `底盘/人体跟踪` |
| 紧急呼叫 | `/voice/listen_mode` 的 `emergency`/`emergency_end` | `/emergency/initiate`（发起）+ `/emergency/abort`（中止） |

---

## 3. 输入接口（其他部门 → 语音侧）

### 3.1 `/vision/scene_objects` —— 视觉模块发布

**频率**：约 2 Hz。**画面中没有物体时不发布。**
**用途**：上报可见物体。语音侧只缓存不立即动作，待用户下次说话时作为上下文使用。

> ⚠️ 必须**持续发布**：语音侧用 3 秒滑动窗口去抖，同类数据需攒够 ≥2 帧才算稳定。只发一帧不会生效。

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `objects` | array | ✅ | 当前帧所有物体；**每个物理物体一个元素**（2 个苹果 = 2 个元素） |
| `objects[].class_name` | string | ✅ | 英文类名，如 `apple`（语音侧按此计数去重） |
| `objects[].name_zh` | string | ✅ | 中文名，如 `苹果`（用于播报） |
| `objects[].graspable` | bool | ✅ | 是否可抓取 |
| `objects[].action` | string | 建议 | `grasp_allowed` / `dialogue_only` |
| `count` | int | 建议 | 物体总数；缺省时按数组长度计算 |
| `objects[].confidence` | float | 可选 | 置信度 0~1 |
| `objects[].camera_xyz` | object | 可选 | 相机坐标，**语音侧不使用**（抓取定位由机械臂自理） |
| `graspable_targets` / `dialogue_only_objects` | array | 可选 | 分类子集；缺省时语音侧自行分类 |
| `stamp_sec` / `age_sec` / `source` / `event` | - | 可选 | 元信息 |

```json
{
  "count": 3,
  "objects": [
    {"class_name": "apple", "name_zh": "苹果", "graspable": true,  "action": "grasp_allowed", "confidence": 0.92},
    {"class_name": "apple", "name_zh": "苹果", "graspable": true,  "action": "grasp_allowed", "confidence": 0.88},
    {"class_name": "cup",   "name_zh": "杯子", "graspable": false, "action": "dialogue_only", "confidence": 0.81}
  ]
}
```

> **最小要求**：每个元素含 `class_name` + `name_zh` + `graspable` 即可工作。同类多个就重复列出，语音侧自动计数。

---

### 3.2 `/vision/emotion_context` —— 情绪模块发布

**频率**：约 2 Hz。**画面中没有人脸时不发布。**
**用途**：上报用户情绪（默认只有一个人）。`intervention_required` 为 true 时语音侧会**自动播报安抚**（带 30 秒冷却，不产生机械动作）。

> ⚠️ 同样需**持续发布**，单帧不满足去抖阈值，不会触发干预。

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `emotion` | string | ✅ | 英文标识，如 `neutral`、`happy`、`low_mood`、`negative_distress` |
| `emotion_zh` | string | ✅ | 中文名，如 `平静`、`情绪低落`（用于播报） |
| `intervention_required` | bool | ✅ | 是否需要安抚；**约定 `low_mood` / `negative_distress` 置 true** |
| `confidence` / `status` / `stamp_sec` / `source` | - | 可选 | 元信息 |

```json
{"emotion": "low_mood", "emotion_zh": "情绪低落", "confidence": 0.91, "intervention_required": true}
```

---

### 3.3 `/voice/listen_mode` —— 麦克风控制

麦克风**唯一**的对外控制入口。有两个订阅者，各自解读、互不通信：

| 节点 | 拿它做什么 |
|------|-----------|
| voice_node | 决定**麦克风开不开、要不要唤醒词**。它只把听到的话转成文字发出去，**从不判断这句话是什么意思** |
| brain_node | 决定**自己进入什么工作状态**：常规对话，还是紧急中止判定 |

#### 六个取值

| `msg.data` | 谁发 | 作用 |
|------------|------|------|
| `"mute"` | **外部节点** | 麦克风关闭，动作期间闭麦 |
| `"unmute"` | **外部节点** | 解除静音 |
| `"emergency"` | **紧急呼叫模块** | 强制开麦 + 免唤醒词 + 启动中止判定（§3.4） |
| `"emergency_end"` | **紧急呼叫模块** | 退出紧急态，恢复原状态 |
| `"continuous"` / `"command"` | brain_node（外部勿发） | 下一句免不免唤醒词 |

未知值按 `command` 处理（fail-open）：宁可多要一次唤醒词，也不把麦克风锁死。

#### 优先级

```
机器人正在说话  ＞  紧急态  ＞  硬静音  ＞  对话轮次
```

对接时会遇到的三件事：

1. **紧急态压过 `mute`。** 机械臂抓取途中老人摔倒，麦克风正被静音——若紧急态不能越过它，用户喊"不用打了"就传不进来。`emergency` 期间静音只是**暂时旁路**，状态仍记着，`emergency_end` 后原样恢复。**机械臂/底盘不必关心紧急流程，照常成对发 `mute`/`unmute`。**
2. **静音是粘性的**，只有 `unmute` 能解除。
3. **机器人说话时一律闭麦**（防止听到自己的声音），紧急态也不例外。所以 TTS 或放歌期间发来的 `emergency`，麦克风要等这段播报放完才开——普通回复几秒，**放歌最长 60 秒**。

> ⚠️ **两对信号都必须成对发送**：`mute`/`unmute`、`emergency`/`emergency_end`。漏发后一半即麦克风卡死。
> 语音侧有兜底超时（150 秒 / 180 秒），但那是安全网，不是可以依赖的流程。

```bash
# 注意 --once：ros2 topic pub 默认 1 Hz 无限循环，不加会让 mute 每秒重发，
# 在别处发的 unmute 一秒内就被压回去。代码里用 publish() 发一次即可。
ros2 topic pub --once /voice/listen_mode std_msgs/msg/String '{data: "mute"}'
ros2 topic pub --once /voice/listen_mode std_msgs/msg/String '{data: "unmute"}'
```

---

### 3.4 紧急呼叫中止 —— 紧急呼叫模块对接 ⭐

紧急呼叫有**两条独立的触发路径**，紧急侧都要支持：

| 路径 | 谁触发 | 入口 |
|------|--------|------|
| **A. 语音侧发起** | 情绪模块识别到负面情绪 | 语音侧发 `/emergency/initiate`（§4.5） |
| **B. 紧急侧自行发起** | 紧急侧自己的摔倒检测等 | 紧急侧直接拨号 |

**`emergency` 是"授权打断"，不是"我在拨号"。** 两条路径都由紧急侧自行决定这次呼叫
允不允许被用户叫停：

| 这次呼叫 | 要不要发 `emergency` | 结果 |
|----------|---------------------|------|
| **允许用户中途叫停** | 发，结束时**成对**发 `emergency_end` | 语音侧开麦监听，听到"不用打了"就发 `/emergency/abort` |
| **不允许被打断**（如已确认的严重警情） | **不发** | 语音侧不介入，麦克风行为照旧，也没有配对负担 |

不发 `emergency` 是完全正当的选择——不是漏了步骤。只有选择"允许打断"时，
才需要订阅 `/emergency/abort`（§4.4）并遵守下面的成对约定。

#### 路径 A 时序（语音侧发起）

| 步 | 谁 | 动作 |
|----|----|------|
| 1 | 情绪侧 | 上报稳定的负面情绪（`intervention_required=true`） |
| 2 | 语音侧 | 播报询问：**"×××，你不舒服吗，需要我帮你联系家人或者医生吗"** |
| 3 | 语音侧 | 等待 **10 秒**，期间开麦免唤醒词监听用户应答 |
| 4a | 语音侧 | 用户**明确拒绝**（"不用"/"我没事"）→ 取消，**不发** initiate |
| 4b | 语音侧 | **无应答或听不清** → 发 `/emergency/initiate`，随后播报"正在帮你给紧急联系人发消息" |
| 5 | 紧急侧 | 收到 initiate，按 `channel` 字段**发送短信或拨打电话** |
| 6 | 紧急侧 | 拨号开始时发 `"emergency"`（进入下面的中止监听） |

> **为什么无应答也发**：老人可能已经痛得说不出话或失去意识——那恰恰是最该联络的情况。
> 只有听到明确拒绝才作罢。两次发起之间有 **30 秒冷却**。

#### 中止时序（两条路径通用）

| 步 | 谁 | 动作 |
|----|----|------|
| 1 | 紧急侧 | **开始拨打电话或发送短信** |
| 2 | 紧急侧 | 发 `"emergency"`（与第 1 步同时，不要等电话拨通） |
| 3 | 语音侧 | 强制开麦、免唤醒词，逐句判定"用户是否要中止求助" |
| 4 | 语音侧 | 判定成立 → 发 `/emergency/abort` |
| 5 | 紧急侧 | 收到后**挂断电话 / 撤回短信** |
| 6 | 双方 | 语音侧自动补发 `emergency_end`；紧急侧流程结束时也发一次（幂等） |

```python
self._mode_pub = self.create_publisher(String, "/voice/listen_mode", 10)
self._mode_pub.publish(String(data="emergency"))      # 拨号开始
...
self._mode_pub.publish(String(data="emergency_end"))  # 流程结束
```

#### 判定方式

两级：**关键词规则**（离线、0.1 秒内，覆盖"不用打了"、"我没事"、"取消"、"按错了"等）→ 规则拿不准时转**大模型语义仲裁**（约 1~2 秒，覆盖"哎呀不至于，我好着呢"这类松散口语）。

> **判定取向是刻意不对称的**：漏判只是多打一通电话，误判会撤销真实求救。
> 因此求救加强句（"救命"、"快叫救护车"、"起不来"）对取消句有**一票否决权**；
> 两者同时出现交大模型仲裁；大模型超时或拿不准一律判定"继续呼叫"。**宁可不撤，也不误撤。**

#### 必须知道的三件事

1. **成对发送。** 漏发 `emergency_end` 会把麦克风钉在强制开麦态，连 `mute` 都压不住（180 秒兜底超时只是安全网）。
2. **一次流程只发一条中止。** 判定成立后语音侧即退出紧急态，要再监听需重发 `emergency`。
3. **中止是语音侧的判定结论，不是身份认证。** 语音侧分辨不出说话的是老人本人还是旁人。若撤销需要更高确认要求，请在收到 `/emergency/abort` 后自行追加流程。

紧急态期间机器人不与用户闲聊、不执行其他语音指令，避免干扰通话。中止成功后会说一句"已经取消紧急呼叫了"。

---

## 4. 输出接口（语音侧 → 其他部门）

### 4.1 `/command` —— 指令总线（多方订阅）⭐

brain_node 把理解后的动作发到这里，**多个执行模块各自过滤自己关心的指令**。
`msg.data` 是 JSON 数组，每条统一为三段式；空数组 `[]` 表示纯对话无动作。

```json
{ "actuator": "执行机构", "action": "动作", "params": { ... } }
```

| actuator | action | params（取值范围 / 选项） | 谁执行 |
|----------|--------|--------------------------|--------|
| 底盘 | 前进 | `speed` 0.05~0.5 m/s（默认 0.2）<br>`distance` 0.1~10 m（默认 1.0） | control_node → `/cmd_vel` |
| 底盘 | 后退 | `speed` 0.05~0.3 m/s（默认 0.15）<br>`distance` 0.1~5 m（默认 0.5） | control_node → `/cmd_vel` |
| 底盘 | 左转 / 右转 | `angle` 5~360 度（默认 90）<br>`speed` 0.1~1.0 rad/s（默认 0.5） | control_node → `/cmd_vel` |
| 底盘 | 停止 | 无 | control_node → 零速 Twist |
| **底盘** | **人体跟踪** | **`状态` = `开始` \| `停止`** | **人体跟踪模块**（control_node 不处理） |
| 机械臂 | 抓取 | `target` = `苹果` \| `香蕉` \| `瓶子` \| `蛋糕` \| `小黄鸭` \| `绿色药盒` \| `大樱桃` | control_node → `/arm/grasp_command` |
| 紧急呼叫 | 中止紧急情况 | `reason`、`detector`、`utterance` | control_node → `/emergency/abort` |

> 「音箱 / 播放歌曲」也在指令集里，但由 brain_node 本地放音，**不下发 `/command`**，
> 订阅方看不到。曲目取自语音侧 `songs/` 目录，增删文件即时生效。

示例（单条与多条）：

```json
[{"actuator":"底盘","action":"人体跟踪","params":{"状态":"停止"}}]
```

```json
[
  {"actuator":"底盘","action":"前进","params":{"speed":0.2,"distance":1.0}},
  {"actuator":"机械臂","action":"抓取","params":{"target":"苹果"}}
]
```

数组按顺序依次执行。参数**总是齐全的**——大模型没给全的会被校验打回重生成，
所以订阅方不必处理缺字段的情况（取值范围同理，越界的指令发不出来）。

> **人体跟踪的两条指令**由人体跟踪模块直接订阅 `/command` 消费，control_node 会静默跳过。
> 用户说"别跟着我了""不用跟着我"→ `状态=停止`；说"跟着我走"→ `状态=开始`。
>
> ⚠️ 注意与「底盘/停止」区分：后者只刹住当前这一下的运动，**跟随模式仍开着**，
> 机器人马上又会跟上来。真正退出跟随只能靠「人体跟踪」+`状态=停止`。

> 除紧急中止外，指令均由大模型生成并经严格校验：执行机构/动作/参数必须命中指令集，
> 否则让大模型重试（最多 2 次），仍失败则丢弃。所以订阅方**不必校验字段合法性**，
> 收到什么就是什么。

---

### 4.2 `/arm/grasp_command` —— 机械臂团队订阅 ⭐

`msg.data` 为**抓取目标的中文名**（纯字符串，非 JSON），七选一：

```
苹果 / 香蕉 / 瓶子 / 蛋糕 / 小黄鸭 / 绿色药盒 / 大樱桃
```

**机械臂团队职责**：收到目标名后自行完成"定位 → 手眼标定 → 抓取"全过程。目标到检测话题的映射由机械臂侧决定，语音侧不关心；若有多个同名物体，抓哪一个也由机械臂侧决定。

---

### 4.3 `/cmd_vel` —— 底盘团队订阅

`geometry_msgs/msg/Twist`，标准消息无特殊约定。control_node 按"距离/速度=时长"持续发布 `linear.x`（正为前）与 `angular.z`（正为左），到时或收到停止后发布全零 Twist。

---

### 4.4 `/emergency/abort` —— 紧急呼叫团队订阅 ⭐

**触发**：语音侧判定用户要中止求助，**一次紧急流程最多一条**。
**语义**：请立即挂断正在拨打的电话 / 撤回正在发送的短信。

| 字段 | 类型 | 说明 |
|------|------|------|
| `event` | string | 固定 `emergency_abort` |
| `action` | string | 固定 `中止紧急情况` |
| `reason` | string | 中止原因，当前只有 `user_cancel` |
| `detector` | string | 判定来源：`rule` / `llm` |
| `utterance` | string | 触发判定的用户原话（≤100 字），**建议留档**以便事后复核 |
| `stamp_sec` | float | Unix 时间戳 |

```json
{
  "event": "emergency_abort", "action": "中止紧急情况",
  "reason": "user_cancel", "detector": "rule",
  "utterance": "不用打了，我没事", "stamp_sec": 1785583532.677988
}
```

**紧急团队职责**：收到后挂断电话/撤回短信并结束流程；建议回发一次 `emergency_end`（重复无害）。若挂断失败（电话已接通等），后续策略由紧急侧决定，语音侧不重发。

> **延迟**：从用户说完话到本话题发出，规则命中约 0.1 秒，大模型仲裁约 1~2 秒（不含语音识别耗时）。

---

### 4.5 `/emergency/initiate` —— 紧急呼叫团队订阅 ⭐

**触发**：情绪模块上报稳定负面情绪，语音侧询问后用户未拒绝（时序见 §3.4 路径 A）。
**语义**：请**发起**紧急联络。按 `channel` 决定是打电话还是发短信。

| 字段 | 类型 | 说明 |
|------|------|------|
| `event` | string | 固定 `emergency_initiate` |
| `channel` | string | **`sms` = 发送短信；`call` = 拨打电话**（当前两档情绪都是 `sms`，`call` 暂未启用但需实现） |
| `reason` | string | 发起原因，当前只有 `negative_emotion` |
| `emotion` | string | 触发的情绪英文标识，如 `negative_distress` |
| `emotion_zh` | string | 情绪中文名，如 `痛苦` |
| `stamp_sec` | float | Unix 时间戳 |

```json
{
  "event": "emergency_initiate", "channel": "call",
  "reason": "negative_emotion",
  "emotion": "negative_distress", "emotion_zh": "痛苦",
  "stamp_sec": 1785588376.951
}
```

**当前的情绪 → 渠道映射**（语音侧 `config.py` 可调）：

| 情绪 | channel | 含义 |
|------|---------|------|
| `negative_distress`（痛苦） | `sms` | 程度重，但仍以短信联络 |
| `low_mood`（情绪低落） | `sms` | 程度轻，发条短信即可 |
| 其他 | —— | **不发起**，语音侧只做大模型安抚 |

**紧急团队职责**：

- 订阅本话题，按 `channel` 发起对应联络。
- **若这次联络允许用户反悔叫停**，发起后发 `"emergency"` 到 `/voice/listen_mode`（语音侧才会开麦监听中止意图）；不允许打断就不发，见 §3.4。
- 语音侧带 **30 秒冷却**。这个间隔不长，**紧急侧务必自行去重/限流**，否则老人情绪持续低落时会收到大量重复短信。

> ⚠️ **这只是"请发起"的请求，不是已核实的警情。** 判断依据是情绪识别 + 用户没出声拒绝，
> 语音侧无法确认老人真的需要急救。若贵侧有更高的核实要求（如先给家属发确认、
> 或接通后由人工判断），请在收到本话题后自行追加。

---

## 5. 对接清单

- [ ] **视觉**：发布 `/vision/scene_objects`，每个物体含 `class_name`+`name_zh`+`graspable`；持续发布，无物体时不发。
- [ ] **情绪**：发布 `/vision/emotion_context`，含 `emotion`+`emotion_zh`+`intervention_required`；持续发布，无人脸时不发。
- [ ] **机械臂**：订阅 `/arm/grasp_command`（中文目标名，七选一）；动作前后**成对**发 `mute`/`unmute`。
- [ ] **底盘**：订阅 `/cmd_vel`；运动前后**成对**发 `mute`/`unmute`。
- [ ] **人体跟踪**：订阅 `/command`，过滤 `actuator=底盘` + `action=人体跟踪`，按 `params.状态` 开始/停止跟随；跟随期间若要闭麦，同样**成对**发 `mute`/`unmute`。
- [ ] **紧急呼叫**：订阅 `/emergency/initiate`，按 `channel` 发短信或打电话；明确本次呼叫**允不允许被用户叫停**——允许则发 `emergency`/`emergency_end`（**成对**）并订阅 `/emergency/abort` 处理挂断，不允许则两者都不做；确认已读 §3.4"必须知道的三件事"与 §4.5 末尾的核实提示；若启用了打断，联调时正例（"不用打了"→应中止）与反例（"快叫救护车"→绝不能中止）各走一遍。
- [ ] **集成**：话题名与消息类型完全一致；`DASHSCOPE_API_KEY` 已配置；音频设备名匹配。
